---
title: Minimum Viable Secure Product - Controls
description: Minimum Viable Secure Product (MVSP) is a minimum security baseline for enterprise-ready products and services.
layout: page
pagination:
  data: mvsp
  size: 1
  alias: control
permalink: '/mvsp.en/{% if control.version != "head" %}{{ control.version }}/{% endif %}'
footer: 'off'
---

{% import "alerts.njk" as alerts %}

Other versions:
<ul>
{%- for m in mvsp %}
  {% if m.version != control.version %}
  <li>
    {% if m.version == "head" %}
      <a href="/mvsp.en/index.html">Latest stable release</a>
    {% else %}
      <a href="/mvsp.en/{{ m.version }}/index.html">
        {{ m.version }}
        {% if m.version == "alpha" %}
          (work in progress)
        {% endif %}
      </a>
    {% endif %}
  </li>
  {% endif %}
{% endfor -%}
</ul>

{% if control.warn %}
   {{ alerts.warn(control.warn) }}
{% endif %}

<div class="adoc">
    {% asciidoc control.content_versioned %}
</div>
