---
title: 'Minimum Viable Secure Product - Controls FAQ'
description: 'FAQ: Additional context on the controls listed in the Minimum Viable Secure Product (MVSP).'
layout: 'page'
pagination:
  data: mvsp
  size: 1
  alias: control
permalink: '/faq-mvsp.en/{% if control.version != "head" %}{{ control.version }}/{% endif %}'
---

{% import "alerts.njk" as alerts %}

Other versions:
<ul>
{%- for m in mvsp %}
  {% if m.version != control.version %}
  <li>
    {% if m.version == "head" %}
      <a href="/faq-mvsp.en/index.html">Latest stable release</a>
    {% else %}
      <a href="/faq-mvsp.en/{{ m.version }}/index.html">
        {{ m.version }}
        {% if m.version == "alpha" %}
          (work in progress)
        {% endif %}
      </a>
    {% endif %}
  </li>
  {% endif %}
{% endfor -%}
</ul>

{% if control.warn %}
   {{ alerts.warn(control.warn) }}
{% endif %}

<div class="adoc">
  {% asciidoc control.faq %}
</div>
